@model IEnumerable<Order>

@{
    ViewData["Title"] = "Orders";
}

@if (ViewBag.CustomerId == 0)
{
    <h1>Orders</h1>
}
else
{
    <h1>Orders @ViewBag.Customer</h1>
}

<table class="table">
    <thead>
        <tr>
            @if (ViewBag.CustomerId == 0)
            {
                <th>
                    Email
                </th>
            }
            <th>
                Name
            </th>
            <th>
               Developer
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @if (ViewBag.CustomerId == 0)
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.Customer.Email)
                    </td>
                }
                <td>
                    @Html.DisplayFor(modelItem => item.Game.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Game.Developer.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
            </tr>
        }
    </tbody>
</table>

<table>
    <tr>
        @if (ViewBag.CustomerId != 0)
        {
            <td>
                <form method="get" asp-controller="Customers" asp-action="Index">
                    <input type="submit" class="btn btn-primary" value="Back to list of customers" />
                </form>
            </td>
        <td></td>
        <td></td>
        <td></td>
        }
        <td>
            <form method="get" asp-controller="Home" asp-action="Index">
                <input type="submit" class="btn btn-primary" value="Home" />
            </form>
        </td>
    </tr>
</table>